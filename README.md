This is a simple host scan using python.

The script request a IP to the user and as the user gives it to him the script scan the network and look for all the hosts that are online, saves them in a list, create a new directory and file to write the results in it.
